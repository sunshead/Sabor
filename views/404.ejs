<%- include header %>

<div id="wrap">
  <div class="container">
    <div class="page-header">
      <h1>Woops! Seems that Sabor could not find the page you want.</h1>
      <% if (locals.error) { %>
        <div><%= locals.error %> </div>
      <% } %>
  </div>
</div>

<%- include footer %>